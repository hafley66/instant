{
  "manifest_version": 3,
  "name": "instant activity",
  "version": "0.2.0",
  "description": "Streams tab navigation, DOM interactions (click/drag/ctrl-click), tab-open sources, selections, and copies to the local instant app (127.0.0.1:8787).",
  "permissions": ["tabs", "webNavigation"],
  "host_permissions": ["http://127.0.0.1:8787/*"],
  "background": { "service_worker": "background.js" },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ]
}
