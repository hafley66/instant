// Structured overlays are compiled out (structuredOverlaysEnabled = false,
// src/terminal.ts:119); no user gesture raises .term-structured-overlay.
import { test } from "@playwright/test";

test.fixme("projects Boop table and list regions onto xterm and expands them", async () => {});
