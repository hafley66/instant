// Sprefa plugin commands. Each function is a Tauri command that the sprefa
// dashboard calls to interact with the sprefa daemon (schema explorer, query
// runner, etc.). Add commands here and register them in lib.rs.

pub mod commands;